<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".Records">

    <!-- TODO: Update blank fragment layout -->

    <TextView
        android:id="@+id/records_textview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/records" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/records_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/records_textview"
        app:layout_constraintBottom_toTopOf="@id/more_games_button" />

    <Button
        android:id="@+id/more_games_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_margin="15dp"
        android:text="@string/more_games"/>



</androidx.constraintlayout.widget.ConstraintLayout>